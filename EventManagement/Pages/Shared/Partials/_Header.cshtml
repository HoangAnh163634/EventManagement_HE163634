@using EventManagement.Services
@inject NotificationService NotificationService
@{
    var userId = User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
    var unreadCount = 0;
    if (!string.IsNullOrEmpty(userId))
    {
        unreadCount = await NotificationService.GetUnreadNotificationCount(userId);
    }
}

<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand text-gold" href="/">Event Management</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/">
                            <i class="fas fa-home me-1"></i>Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/Events">
                            <i class="fas fa-calendar-alt me-1"></i>Events
                        </a>
                    </li>

                    
                    @if (Context.Session.GetInt32("UserId").HasValue)
                    {
                        var userName = Context.Session.GetString("UserName") ?? "User";
                        var userRole = Context.Session.GetString("UserRole") ?? "Attendee";
                        
                        @if ((userRole == "Admin" || userRole == "Organizer") && Context.Session.GetString("UserEmailVerified") == "true")
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white" href="/Events/Create">
                                    <i class="fas fa-plus-circle me-1"></i>Create Event
                                </a>
                            </li>
                        }
                        else if ((userRole == "Admin" || userRole == "Organizer") && Context.Session.GetString("UserEmailVerified") == "false")
                        {
                            <li class="nav-item">
                                <a class="nav-link text-secondary disabled" tabindex="-1" aria-disabled="true" title="Bạn cần xác thực email để tạo sự kiện">
                                    <i class="fas fa-plus-circle me-1"></i>Create Event (Xác thực email)
                                </a>
                            </li>
                        }

                        <!-- Notifications -->
                        <li class="nav-item me-2">
                            <a class="nav-link text-white position-relative" href="/Notifications">
                                <i class="fas fa-bell"></i>
                                @if (unreadCount > 0)
                                {
                                    <span class="badge bg-danger">@unreadCount</span>
                                }
                            </a>
                        </li>
                        
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-gold" href="#" id="navbarDropdown" role="button" onclick="toggleDropdown(event)" aria-expanded="false">
                                <i class="fas fa-user-circle me-1"></i>@userName
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" id="userDropdownMenu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item text-white" href="/Account/Profile">
                                    <i class="fas fa-user me-2"></i>My Profile
                                </a></li>
                                <li><a class="dropdown-item text-white" href="/Events/MyEvents">
                                    <i class="fas fa-calendar me-2"></i>My Events
                                </a></li>
                                <li><a class="dropdown-item text-white" href="/Registrations/MyRegistrations">
                                    <i class="fas fa-ticket-alt me-2"></i>My Registrations
                                </a></li>
                                @if (userRole == "Admin")
                                {
                                    <li><hr class="dropdown-divider" style="border-color: #2F2F2F;"></li>
                                    <li><a class="dropdown-item text-gold" href="/Admin/Dashboard">
                                        <i class="fas fa-cog me-2"></i>Admin Panel
                                    </a></li>
                                }
                                <li><hr class="dropdown-divider" style="border-color: #2F2F2F;"></li>
                                <li>
                                    <a href="/Account/Logout" class="dropdown-item text-white" 
                                       onclick="return confirm('Are you sure you want to logout?')">
                                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                                    </a>
                                </li>
                            </ul>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/Account/Login">
                                <i class="fas fa-sign-in-alt me-1"></i>Login
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-gold ms-2 px-3" href="/Account/Register">
                                <i class="fas fa-user-plus me-1"></i>Register
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>
</header>
